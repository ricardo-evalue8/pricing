{# Testimonial #}
<style>
.widget {
	margin-bottom: 0px;
}
</style>

<section id="testimonial" class="alizarin">
	<div class="container">
		<div class="row">
			<div class="col-lg-12">
				<div class="center">
					<section data-bolt-field="templatefields[section_1]">{{ record.templatefields.section_1 }}</section>

					{# Get title from config #}
					{% for name,value in title %}
						<h2>{{ value }}</h2>
					{% endfor %}

					{# Get description from config #}
					{% for name,values in description %}
						<p>{{ values }}</p>
					{% endfor %}

				</div>
				<div class="gap"></div>
				<div class="row">

					{# Get 4 random pricing #}
					{% setcontent pricing = "pricing/random/4" %}
					{% for pricing in pricing %}
					<div class="col-md-6">
						<blockquote>
							<p>{{ pricing.text }}</p>
							<small>{{ pricing.name }}</small>
						</blockquote>
					</div>
					{% endfor %}

				</div>
			</div>
		</div>
	</div>
</section><!--/#testimonial-->
